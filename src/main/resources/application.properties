# Server Port
server.port=8086

# JSP View Resolver
spring.mvc.view.prefix=/WEB-INF/views/
spring.mvc.view.suffix=.jsp

# MySQL Datasource Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/quizmgmt
spring.datasource.username=root
spring.datasource.password=sweta@04x
# The driver-class-name is auto-detected by modern Spring Boot, so it's not needed.

# Hibernate Properties for MySQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect